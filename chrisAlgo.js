// one function using Math.random() calc value of pi
// should return as close to pi as possible

// calculate two points x and y using Math.random()
// get length of third point of triangle using Pythagoriam theorem a**2 * b**2 = c**2 if that third point is less than the radius of the circle add to areaPoints, and either way add to totalPoints. areaPoint/TotalPoints * 4 = area of circle. pi = area * r**2 ??

function returnCloseToPi() {
  
}

console.log(Math.random())